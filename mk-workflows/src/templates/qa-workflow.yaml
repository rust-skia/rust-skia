# Generated by mk-workflows.

name: '$[[workflowName]]'
on:
  push:
    branches-ignore:
      - release
  schedule:
    cron: '0 0 * * *'
    branches-ignore:
      - release

concurrency:
  group: '$[[workflowName]]-${{ github.ref }}'
  cancel-in-progress: true

jobs:
