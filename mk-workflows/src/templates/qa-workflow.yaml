# Generated by mk-workflows.

name: '$[[workflowName]]'
on:
  push:
    branches-ignore:
      - release
  pull_request:
    branches-ignore:
      - release
  schedule:
    - cron: '0 0 * * *'

concurrency:
  group: '$[[workflowName]]-${{ github.ref }}'
  cancel-in-progress: true

jobs:
