[package]
name = "wasm-unknown-example"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[workspace]

[dependencies]
skia-safe = { path = "../skia-safe", default-features = false }
wasm-bindgen = "0.2"
