use crate::{Bitmap, Data, EncodedImageFormat};

// TODO: SkEncodeImage()

// TODO: SkencodePixmap()

pub fn encode_bitmap(src: &Bitmap, format: EncodedImageFormat, quality: usize) -> Data {
    unimplemented!()
}
