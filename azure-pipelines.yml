jobs:

- template: azure-pipelines-template.yml
  parameters:
    platform: Linux
    image: ubuntu-16.04

- template: azure-pipelines-template.yml
  parameters:
    platform: macOS
    image: macOS-10.14

- template: azure-pipelines-template.yml
  parameters:
    platform: Windows
    image: vs2017-win2016

