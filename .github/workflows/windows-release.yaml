# Generated by mk-workflows.

name: 'windows-release'
on:
  push:
    branches:
      - release

concurrency:
  group: 'windows-release-${{ github.ref }}'
  cancel-in-progress: true

jobs:
  windows-release-release:
    runs-on: windows-2022
    # Containers are not supported on Windows.
    # container: ghcr.io/pragmatrix/rust-skia-windows:latest
    env: 
      SKIA_DEBUG: 0
    
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    
    - name: Install Rust
      uses: hecrj/setup-rust-action@master
      with:
        rust-version: stable
    
    - name: Install Clippy
      run: rustup component add clippy
      shell: bash
    
    - name: Python Version
      run: python --version
    
    - name: Install LLVM
      run: choco install llvm
    - name: 'Install Rust target x86_64-pc-windows-msvc'
      shell: bash
      run: |
        rustup target add x86_64-pc-windows-msvc
    
    - name: 'Build all targets in skia-safe for x86_64-pc-windows-msvc with features '
      shell: bash
      run: |
        if [ "false" == "true" ]; then
          TARGET=x86_64-pc-windows-msvc
          TARGET=${TARGET//-/_}
          export CC_${TARGET}=x86_64-pc-windows-msvc26-clang.exe
          export CXX_${TARGET}=x86_64-pc-windows-msvc26-clang++.exe
          TARGET_UPPERCASE=`echo "${TARGET}" | tr [a-z] [A-Z]`
          export CARGO_TARGET_${TARGET_UPPERCASE}_LINKER=x86_64-pc-windows-msvc26-clang.exe
          echo "Set CC, CXX, and CARGO_LINKER target specific environment variables for Android"
        fi
        if [ "false" == "true" ]; then
          source /emsdk/emsdk_env.sh
        fi
        cargo clean
        cargo build -p skia-safe --all-targets --release --features "" --target x86_64-pc-windows-msvc
        echo "SKIA_BINARIES_TAG=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/tag.txt")" >> ${GITHUB_ENV}
        echo "SKIA_BINARIES_KEY=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/key.txt")" >> ${GITHUB_ENV}
        echo "SKIA_STAGING_PATH=${BUILD_ARTIFACTSTAGINGDIRECTORY}" >> ${GITHUB_ENV}
      env:
        BUILD_ARTIFACTSTAGINGDIRECTORY: ${{ runner.temp }}
        EMCC_CFLAGS: "-s ERROR_ON_UNDEFINED_SYMBOLS=0 -s MAX_WEBGL_VERSION=2"
    
    - name: 'Run Clippy'
      shell: bash
      if: true
      run: |
        cargo clippy --release --features "" --all-targets --target x86_64-pc-windows-msvc -- -D warnings
    
    - name: 'Test all workspace projects'
      shell: bash
      if: true
      run: |
        cargo test --all --release --features "" --all-targets --target x86_64-pc-windows-msvc -- --nocapture
      
    - name: 'Generate skia-org example images'
      shell: bash
      if: false
      run: |
        cargo run --release --features "" --target x86_64-pc-windows-msvc "${{ env.SKIA_STAGING_PATH }}/skia-org" 
    
    - name: 'Upload skia-org example images'
      if: false
      uses: actions/upload-artifact@v2
      with:
        name: skia-org-images-x86_64-pc-windows-msvc
        path: ${{ env.SKIA_STAGING_PATH }}/skia-org
    
    - name: 'Compress binaries'
      if: true
      uses: master-atul/tar-action@v1.0.2
      with:
        command: c
        cwd: '${{ env.SKIA_STAGING_PATH }}'
        files: 'skia-binaries'
        outPath: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
    
    - name: 'Release binaries'
      if: true
      uses: pragmatrix/release-action@exp
      with:
        owner: rust-skia
        repo: skia-binaries
        allowUpdates: true
        replacesArtifacts: true
        tag: '${{ env.SKIA_BINARIES_TAG }}'
        artifacts: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
        artifactErrorsFailBuild: true
        token: ${{ secrets.RUST_SKIA_RELEASE_TOKEN }}
        prerelease: true
  windows-release-release-gl:
    runs-on: windows-2022
    # Containers are not supported on Windows.
    # container: ghcr.io/pragmatrix/rust-skia-windows:latest
    env: 
      SKIA_DEBUG: 0
    
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    
    - name: Install Rust
      uses: hecrj/setup-rust-action@master
      with:
        rust-version: stable
    
    - name: Install Clippy
      run: rustup component add clippy
      shell: bash
    
    - name: Python Version
      run: python --version
    
    - name: Install LLVM
      run: choco install llvm
    - name: 'Install Rust target x86_64-pc-windows-msvc'
      shell: bash
      run: |
        rustup target add x86_64-pc-windows-msvc
    
    - name: 'Build all targets in skia-safe for x86_64-pc-windows-msvc with features gl'
      shell: bash
      run: |
        if [ "false" == "true" ]; then
          TARGET=x86_64-pc-windows-msvc
          TARGET=${TARGET//-/_}
          export CC_${TARGET}=x86_64-pc-windows-msvc26-clang.exe
          export CXX_${TARGET}=x86_64-pc-windows-msvc26-clang++.exe
          TARGET_UPPERCASE=`echo "${TARGET}" | tr [a-z] [A-Z]`
          export CARGO_TARGET_${TARGET_UPPERCASE}_LINKER=x86_64-pc-windows-msvc26-clang.exe
          echo "Set CC, CXX, and CARGO_LINKER target specific environment variables for Android"
        fi
        if [ "false" == "true" ]; then
          source /emsdk/emsdk_env.sh
        fi
        cargo clean
        cargo build -p skia-safe --all-targets --release --features "gl" --target x86_64-pc-windows-msvc
        echo "SKIA_BINARIES_TAG=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/tag.txt")" >> ${GITHUB_ENV}
        echo "SKIA_BINARIES_KEY=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/key.txt")" >> ${GITHUB_ENV}
        echo "SKIA_STAGING_PATH=${BUILD_ARTIFACTSTAGINGDIRECTORY}" >> ${GITHUB_ENV}
      env:
        BUILD_ARTIFACTSTAGINGDIRECTORY: ${{ runner.temp }}
        EMCC_CFLAGS: "-s ERROR_ON_UNDEFINED_SYMBOLS=0 -s MAX_WEBGL_VERSION=2"
    
    - name: 'Run Clippy'
      shell: bash
      if: true
      run: |
        cargo clippy --release --features "gl" --all-targets --target x86_64-pc-windows-msvc -- -D warnings
    
    - name: 'Test all workspace projects'
      shell: bash
      if: true
      run: |
        cargo test --all --release --features "gl" --all-targets --target x86_64-pc-windows-msvc -- --nocapture
      
    - name: 'Generate skia-org example images'
      shell: bash
      if: false
      run: |
        cargo run --release --features "gl" --target x86_64-pc-windows-msvc "${{ env.SKIA_STAGING_PATH }}/skia-org" 
    
    - name: 'Upload skia-org example images'
      if: false
      uses: actions/upload-artifact@v2
      with:
        name: skia-org-images-x86_64-pc-windows-msvc
        path: ${{ env.SKIA_STAGING_PATH }}/skia-org
    
    - name: 'Compress binaries'
      if: true
      uses: master-atul/tar-action@v1.0.2
      with:
        command: c
        cwd: '${{ env.SKIA_STAGING_PATH }}'
        files: 'skia-binaries'
        outPath: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
    
    - name: 'Release binaries'
      if: true
      uses: pragmatrix/release-action@exp
      with:
        owner: rust-skia
        repo: skia-binaries
        allowUpdates: true
        replacesArtifacts: true
        tag: '${{ env.SKIA_BINARIES_TAG }}'
        artifacts: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
        artifactErrorsFailBuild: true
        token: ${{ secrets.RUST_SKIA_RELEASE_TOKEN }}
        prerelease: true
  windows-release-release-vulkan:
    runs-on: windows-2022
    # Containers are not supported on Windows.
    # container: ghcr.io/pragmatrix/rust-skia-windows:latest
    env: 
      SKIA_DEBUG: 0
    
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    
    - name: Install Rust
      uses: hecrj/setup-rust-action@master
      with:
        rust-version: stable
    
    - name: Install Clippy
      run: rustup component add clippy
      shell: bash
    
    - name: Python Version
      run: python --version
    
    - name: Install LLVM
      run: choco install llvm
    - name: 'Install Rust target x86_64-pc-windows-msvc'
      shell: bash
      run: |
        rustup target add x86_64-pc-windows-msvc
    
    - name: 'Build all targets in skia-safe for x86_64-pc-windows-msvc with features vulkan'
      shell: bash
      run: |
        if [ "false" == "true" ]; then
          TARGET=x86_64-pc-windows-msvc
          TARGET=${TARGET//-/_}
          export CC_${TARGET}=x86_64-pc-windows-msvc26-clang.exe
          export CXX_${TARGET}=x86_64-pc-windows-msvc26-clang++.exe
          TARGET_UPPERCASE=`echo "${TARGET}" | tr [a-z] [A-Z]`
          export CARGO_TARGET_${TARGET_UPPERCASE}_LINKER=x86_64-pc-windows-msvc26-clang.exe
          echo "Set CC, CXX, and CARGO_LINKER target specific environment variables for Android"
        fi
        if [ "false" == "true" ]; then
          source /emsdk/emsdk_env.sh
        fi
        cargo clean
        cargo build -p skia-safe --all-targets --release --features "vulkan" --target x86_64-pc-windows-msvc
        echo "SKIA_BINARIES_TAG=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/tag.txt")" >> ${GITHUB_ENV}
        echo "SKIA_BINARIES_KEY=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/key.txt")" >> ${GITHUB_ENV}
        echo "SKIA_STAGING_PATH=${BUILD_ARTIFACTSTAGINGDIRECTORY}" >> ${GITHUB_ENV}
      env:
        BUILD_ARTIFACTSTAGINGDIRECTORY: ${{ runner.temp }}
        EMCC_CFLAGS: "-s ERROR_ON_UNDEFINED_SYMBOLS=0 -s MAX_WEBGL_VERSION=2"
    
    - name: 'Run Clippy'
      shell: bash
      if: true
      run: |
        cargo clippy --release --features "vulkan" --all-targets --target x86_64-pc-windows-msvc -- -D warnings
    
    - name: 'Test all workspace projects'
      shell: bash
      if: true
      run: |
        cargo test --all --release --features "vulkan" --all-targets --target x86_64-pc-windows-msvc -- --nocapture
      
    - name: 'Generate skia-org example images'
      shell: bash
      if: false
      run: |
        cargo run --release --features "vulkan" --target x86_64-pc-windows-msvc "${{ env.SKIA_STAGING_PATH }}/skia-org" 
    
    - name: 'Upload skia-org example images'
      if: false
      uses: actions/upload-artifact@v2
      with:
        name: skia-org-images-x86_64-pc-windows-msvc
        path: ${{ env.SKIA_STAGING_PATH }}/skia-org
    
    - name: 'Compress binaries'
      if: true
      uses: master-atul/tar-action@v1.0.2
      with:
        command: c
        cwd: '${{ env.SKIA_STAGING_PATH }}'
        files: 'skia-binaries'
        outPath: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
    
    - name: 'Release binaries'
      if: true
      uses: pragmatrix/release-action@exp
      with:
        owner: rust-skia
        repo: skia-binaries
        allowUpdates: true
        replacesArtifacts: true
        tag: '${{ env.SKIA_BINARIES_TAG }}'
        artifacts: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
        artifactErrorsFailBuild: true
        token: ${{ secrets.RUST_SKIA_RELEASE_TOKEN }}
        prerelease: true
  windows-release-release-textlayout:
    runs-on: windows-2022
    # Containers are not supported on Windows.
    # container: ghcr.io/pragmatrix/rust-skia-windows:latest
    env: 
      SKIA_DEBUG: 0
    
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    
    - name: Install Rust
      uses: hecrj/setup-rust-action@master
      with:
        rust-version: stable
    
    - name: Install Clippy
      run: rustup component add clippy
      shell: bash
    
    - name: Python Version
      run: python --version
    
    - name: Install LLVM
      run: choco install llvm
    - name: 'Install Rust target x86_64-pc-windows-msvc'
      shell: bash
      run: |
        rustup target add x86_64-pc-windows-msvc
    
    - name: 'Build all targets in skia-safe for x86_64-pc-windows-msvc with features textlayout'
      shell: bash
      run: |
        if [ "false" == "true" ]; then
          TARGET=x86_64-pc-windows-msvc
          TARGET=${TARGET//-/_}
          export CC_${TARGET}=x86_64-pc-windows-msvc26-clang.exe
          export CXX_${TARGET}=x86_64-pc-windows-msvc26-clang++.exe
          TARGET_UPPERCASE=`echo "${TARGET}" | tr [a-z] [A-Z]`
          export CARGO_TARGET_${TARGET_UPPERCASE}_LINKER=x86_64-pc-windows-msvc26-clang.exe
          echo "Set CC, CXX, and CARGO_LINKER target specific environment variables for Android"
        fi
        if [ "false" == "true" ]; then
          source /emsdk/emsdk_env.sh
        fi
        cargo clean
        cargo build -p skia-safe --all-targets --release --features "textlayout" --target x86_64-pc-windows-msvc
        echo "SKIA_BINARIES_TAG=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/tag.txt")" >> ${GITHUB_ENV}
        echo "SKIA_BINARIES_KEY=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/key.txt")" >> ${GITHUB_ENV}
        echo "SKIA_STAGING_PATH=${BUILD_ARTIFACTSTAGINGDIRECTORY}" >> ${GITHUB_ENV}
      env:
        BUILD_ARTIFACTSTAGINGDIRECTORY: ${{ runner.temp }}
        EMCC_CFLAGS: "-s ERROR_ON_UNDEFINED_SYMBOLS=0 -s MAX_WEBGL_VERSION=2"
    
    - name: 'Run Clippy'
      shell: bash
      if: true
      run: |
        cargo clippy --release --features "textlayout" --all-targets --target x86_64-pc-windows-msvc -- -D warnings
    
    - name: 'Test all workspace projects'
      shell: bash
      if: true
      run: |
        cargo test --all --release --features "textlayout" --all-targets --target x86_64-pc-windows-msvc -- --nocapture
      
    - name: 'Generate skia-org example images'
      shell: bash
      if: false
      run: |
        cargo run --release --features "textlayout" --target x86_64-pc-windows-msvc "${{ env.SKIA_STAGING_PATH }}/skia-org" 
    
    - name: 'Upload skia-org example images'
      if: false
      uses: actions/upload-artifact@v2
      with:
        name: skia-org-images-x86_64-pc-windows-msvc
        path: ${{ env.SKIA_STAGING_PATH }}/skia-org
    
    - name: 'Compress binaries'
      if: true
      uses: master-atul/tar-action@v1.0.2
      with:
        command: c
        cwd: '${{ env.SKIA_STAGING_PATH }}'
        files: 'skia-binaries'
        outPath: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
    
    - name: 'Release binaries'
      if: true
      uses: pragmatrix/release-action@exp
      with:
        owner: rust-skia
        repo: skia-binaries
        allowUpdates: true
        replacesArtifacts: true
        tag: '${{ env.SKIA_BINARIES_TAG }}'
        artifacts: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
        artifactErrorsFailBuild: true
        token: ${{ secrets.RUST_SKIA_RELEASE_TOKEN }}
        prerelease: true
  windows-release-release-gl-textlayout:
    runs-on: windows-2022
    # Containers are not supported on Windows.
    # container: ghcr.io/pragmatrix/rust-skia-windows:latest
    env: 
      SKIA_DEBUG: 0
    
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    
    - name: Install Rust
      uses: hecrj/setup-rust-action@master
      with:
        rust-version: stable
    
    - name: Install Clippy
      run: rustup component add clippy
      shell: bash
    
    - name: Python Version
      run: python --version
    
    - name: Install LLVM
      run: choco install llvm
    - name: 'Install Rust target x86_64-pc-windows-msvc'
      shell: bash
      run: |
        rustup target add x86_64-pc-windows-msvc
    
    - name: 'Build all targets in skia-safe for x86_64-pc-windows-msvc with features gl,textlayout'
      shell: bash
      run: |
        if [ "false" == "true" ]; then
          TARGET=x86_64-pc-windows-msvc
          TARGET=${TARGET//-/_}
          export CC_${TARGET}=x86_64-pc-windows-msvc26-clang.exe
          export CXX_${TARGET}=x86_64-pc-windows-msvc26-clang++.exe
          TARGET_UPPERCASE=`echo "${TARGET}" | tr [a-z] [A-Z]`
          export CARGO_TARGET_${TARGET_UPPERCASE}_LINKER=x86_64-pc-windows-msvc26-clang.exe
          echo "Set CC, CXX, and CARGO_LINKER target specific environment variables for Android"
        fi
        if [ "false" == "true" ]; then
          source /emsdk/emsdk_env.sh
        fi
        cargo clean
        cargo build -p skia-safe --all-targets --release --features "gl,textlayout" --target x86_64-pc-windows-msvc
        echo "SKIA_BINARIES_TAG=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/tag.txt")" >> ${GITHUB_ENV}
        echo "SKIA_BINARIES_KEY=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/key.txt")" >> ${GITHUB_ENV}
        echo "SKIA_STAGING_PATH=${BUILD_ARTIFACTSTAGINGDIRECTORY}" >> ${GITHUB_ENV}
      env:
        BUILD_ARTIFACTSTAGINGDIRECTORY: ${{ runner.temp }}
        EMCC_CFLAGS: "-s ERROR_ON_UNDEFINED_SYMBOLS=0 -s MAX_WEBGL_VERSION=2"
    
    - name: 'Run Clippy'
      shell: bash
      if: true
      run: |
        cargo clippy --release --features "gl,textlayout" --all-targets --target x86_64-pc-windows-msvc -- -D warnings
    
    - name: 'Test all workspace projects'
      shell: bash
      if: true
      run: |
        cargo test --all --release --features "gl,textlayout" --all-targets --target x86_64-pc-windows-msvc -- --nocapture
      
    - name: 'Generate skia-org example images'
      shell: bash
      if: false
      run: |
        cargo run --release --features "gl,textlayout" --target x86_64-pc-windows-msvc "${{ env.SKIA_STAGING_PATH }}/skia-org" 
    
    - name: 'Upload skia-org example images'
      if: false
      uses: actions/upload-artifact@v2
      with:
        name: skia-org-images-x86_64-pc-windows-msvc
        path: ${{ env.SKIA_STAGING_PATH }}/skia-org
    
    - name: 'Compress binaries'
      if: true
      uses: master-atul/tar-action@v1.0.2
      with:
        command: c
        cwd: '${{ env.SKIA_STAGING_PATH }}'
        files: 'skia-binaries'
        outPath: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
    
    - name: 'Release binaries'
      if: true
      uses: pragmatrix/release-action@exp
      with:
        owner: rust-skia
        repo: skia-binaries
        allowUpdates: true
        replacesArtifacts: true
        tag: '${{ env.SKIA_BINARIES_TAG }}'
        artifacts: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
        artifactErrorsFailBuild: true
        token: ${{ secrets.RUST_SKIA_RELEASE_TOKEN }}
        prerelease: true
  windows-release-release-vulkan-textlayout:
    runs-on: windows-2022
    # Containers are not supported on Windows.
    # container: ghcr.io/pragmatrix/rust-skia-windows:latest
    env: 
      SKIA_DEBUG: 0
    
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    
    - name: Install Rust
      uses: hecrj/setup-rust-action@master
      with:
        rust-version: stable
    
    - name: Install Clippy
      run: rustup component add clippy
      shell: bash
    
    - name: Python Version
      run: python --version
    
    - name: Install LLVM
      run: choco install llvm
    - name: 'Install Rust target x86_64-pc-windows-msvc'
      shell: bash
      run: |
        rustup target add x86_64-pc-windows-msvc
    
    - name: 'Build all targets in skia-safe for x86_64-pc-windows-msvc with features vulkan,textlayout'
      shell: bash
      run: |
        if [ "false" == "true" ]; then
          TARGET=x86_64-pc-windows-msvc
          TARGET=${TARGET//-/_}
          export CC_${TARGET}=x86_64-pc-windows-msvc26-clang.exe
          export CXX_${TARGET}=x86_64-pc-windows-msvc26-clang++.exe
          TARGET_UPPERCASE=`echo "${TARGET}" | tr [a-z] [A-Z]`
          export CARGO_TARGET_${TARGET_UPPERCASE}_LINKER=x86_64-pc-windows-msvc26-clang.exe
          echo "Set CC, CXX, and CARGO_LINKER target specific environment variables for Android"
        fi
        if [ "false" == "true" ]; then
          source /emsdk/emsdk_env.sh
        fi
        cargo clean
        cargo build -p skia-safe --all-targets --release --features "vulkan,textlayout" --target x86_64-pc-windows-msvc
        echo "SKIA_BINARIES_TAG=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/tag.txt")" >> ${GITHUB_ENV}
        echo "SKIA_BINARIES_KEY=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/key.txt")" >> ${GITHUB_ENV}
        echo "SKIA_STAGING_PATH=${BUILD_ARTIFACTSTAGINGDIRECTORY}" >> ${GITHUB_ENV}
      env:
        BUILD_ARTIFACTSTAGINGDIRECTORY: ${{ runner.temp }}
        EMCC_CFLAGS: "-s ERROR_ON_UNDEFINED_SYMBOLS=0 -s MAX_WEBGL_VERSION=2"
    
    - name: 'Run Clippy'
      shell: bash
      if: true
      run: |
        cargo clippy --release --features "vulkan,textlayout" --all-targets --target x86_64-pc-windows-msvc -- -D warnings
    
    - name: 'Test all workspace projects'
      shell: bash
      if: true
      run: |
        cargo test --all --release --features "vulkan,textlayout" --all-targets --target x86_64-pc-windows-msvc -- --nocapture
      
    - name: 'Generate skia-org example images'
      shell: bash
      if: false
      run: |
        cargo run --release --features "vulkan,textlayout" --target x86_64-pc-windows-msvc "${{ env.SKIA_STAGING_PATH }}/skia-org" 
    
    - name: 'Upload skia-org example images'
      if: false
      uses: actions/upload-artifact@v2
      with:
        name: skia-org-images-x86_64-pc-windows-msvc
        path: ${{ env.SKIA_STAGING_PATH }}/skia-org
    
    - name: 'Compress binaries'
      if: true
      uses: master-atul/tar-action@v1.0.2
      with:
        command: c
        cwd: '${{ env.SKIA_STAGING_PATH }}'
        files: 'skia-binaries'
        outPath: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
    
    - name: 'Release binaries'
      if: true
      uses: pragmatrix/release-action@exp
      with:
        owner: rust-skia
        repo: skia-binaries
        allowUpdates: true
        replacesArtifacts: true
        tag: '${{ env.SKIA_BINARIES_TAG }}'
        artifacts: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
        artifactErrorsFailBuild: true
        token: ${{ secrets.RUST_SKIA_RELEASE_TOKEN }}
        prerelease: true
  windows-release-release-gl-vulkan-textlayout:
    runs-on: windows-2022
    # Containers are not supported on Windows.
    # container: ghcr.io/pragmatrix/rust-skia-windows:latest
    env: 
      SKIA_DEBUG: 0
    
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    
    - name: Install Rust
      uses: hecrj/setup-rust-action@master
      with:
        rust-version: stable
    
    - name: Install Clippy
      run: rustup component add clippy
      shell: bash
    
    - name: Python Version
      run: python --version
    
    - name: Install LLVM
      run: choco install llvm
    - name: 'Install Rust target x86_64-pc-windows-msvc'
      shell: bash
      run: |
        rustup target add x86_64-pc-windows-msvc
    
    - name: 'Build all targets in skia-safe for x86_64-pc-windows-msvc with features gl,vulkan,textlayout'
      shell: bash
      run: |
        if [ "false" == "true" ]; then
          TARGET=x86_64-pc-windows-msvc
          TARGET=${TARGET//-/_}
          export CC_${TARGET}=x86_64-pc-windows-msvc26-clang.exe
          export CXX_${TARGET}=x86_64-pc-windows-msvc26-clang++.exe
          TARGET_UPPERCASE=`echo "${TARGET}" | tr [a-z] [A-Z]`
          export CARGO_TARGET_${TARGET_UPPERCASE}_LINKER=x86_64-pc-windows-msvc26-clang.exe
          echo "Set CC, CXX, and CARGO_LINKER target specific environment variables for Android"
        fi
        if [ "false" == "true" ]; then
          source /emsdk/emsdk_env.sh
        fi
        cargo clean
        cargo build -p skia-safe --all-targets --release --features "gl,vulkan,textlayout" --target x86_64-pc-windows-msvc
        echo "SKIA_BINARIES_TAG=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/tag.txt")" >> ${GITHUB_ENV}
        echo "SKIA_BINARIES_KEY=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/key.txt")" >> ${GITHUB_ENV}
        echo "SKIA_STAGING_PATH=${BUILD_ARTIFACTSTAGINGDIRECTORY}" >> ${GITHUB_ENV}
      env:
        BUILD_ARTIFACTSTAGINGDIRECTORY: ${{ runner.temp }}
        EMCC_CFLAGS: "-s ERROR_ON_UNDEFINED_SYMBOLS=0 -s MAX_WEBGL_VERSION=2"
    
    - name: 'Run Clippy'
      shell: bash
      if: true
      run: |
        cargo clippy --release --features "gl,vulkan,textlayout" --all-targets --target x86_64-pc-windows-msvc -- -D warnings
    
    - name: 'Test all workspace projects'
      shell: bash
      if: true
      run: |
        cargo test --all --release --features "gl,vulkan,textlayout" --all-targets --target x86_64-pc-windows-msvc -- --nocapture
      
    - name: 'Generate skia-org example images'
      shell: bash
      if: false
      run: |
        cargo run --release --features "gl,vulkan,textlayout" --target x86_64-pc-windows-msvc "${{ env.SKIA_STAGING_PATH }}/skia-org" 
    
    - name: 'Upload skia-org example images'
      if: false
      uses: actions/upload-artifact@v2
      with:
        name: skia-org-images-x86_64-pc-windows-msvc
        path: ${{ env.SKIA_STAGING_PATH }}/skia-org
    
    - name: 'Compress binaries'
      if: true
      uses: master-atul/tar-action@v1.0.2
      with:
        command: c
        cwd: '${{ env.SKIA_STAGING_PATH }}'
        files: 'skia-binaries'
        outPath: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
    
    - name: 'Release binaries'
      if: true
      uses: pragmatrix/release-action@exp
      with:
        owner: rust-skia
        repo: skia-binaries
        allowUpdates: true
        replacesArtifacts: true
        tag: '${{ env.SKIA_BINARIES_TAG }}'
        artifacts: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
        artifactErrorsFailBuild: true
        token: ${{ secrets.RUST_SKIA_RELEASE_TOKEN }}
        prerelease: true
  windows-release-release-d3d:
    runs-on: windows-2022
    # Containers are not supported on Windows.
    # container: ghcr.io/pragmatrix/rust-skia-windows:latest
    env: 
      SKIA_DEBUG: 0
    
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    
    - name: Install Rust
      uses: hecrj/setup-rust-action@master
      with:
        rust-version: stable
    
    - name: Install Clippy
      run: rustup component add clippy
      shell: bash
    
    - name: Python Version
      run: python --version
    
    - name: Install LLVM
      run: choco install llvm
    - name: 'Install Rust target x86_64-pc-windows-msvc'
      shell: bash
      run: |
        rustup target add x86_64-pc-windows-msvc
    
    - name: 'Build all targets in skia-safe for x86_64-pc-windows-msvc with features d3d'
      shell: bash
      run: |
        if [ "false" == "true" ]; then
          TARGET=x86_64-pc-windows-msvc
          TARGET=${TARGET//-/_}
          export CC_${TARGET}=x86_64-pc-windows-msvc26-clang.exe
          export CXX_${TARGET}=x86_64-pc-windows-msvc26-clang++.exe
          TARGET_UPPERCASE=`echo "${TARGET}" | tr [a-z] [A-Z]`
          export CARGO_TARGET_${TARGET_UPPERCASE}_LINKER=x86_64-pc-windows-msvc26-clang.exe
          echo "Set CC, CXX, and CARGO_LINKER target specific environment variables for Android"
        fi
        if [ "false" == "true" ]; then
          source /emsdk/emsdk_env.sh
        fi
        cargo clean
        cargo build -p skia-safe --all-targets --release --features "d3d" --target x86_64-pc-windows-msvc
        echo "SKIA_BINARIES_TAG=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/tag.txt")" >> ${GITHUB_ENV}
        echo "SKIA_BINARIES_KEY=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/key.txt")" >> ${GITHUB_ENV}
        echo "SKIA_STAGING_PATH=${BUILD_ARTIFACTSTAGINGDIRECTORY}" >> ${GITHUB_ENV}
      env:
        BUILD_ARTIFACTSTAGINGDIRECTORY: ${{ runner.temp }}
        EMCC_CFLAGS: "-s ERROR_ON_UNDEFINED_SYMBOLS=0 -s MAX_WEBGL_VERSION=2"
    
    - name: 'Run Clippy'
      shell: bash
      if: true
      run: |
        cargo clippy --release --features "d3d" --all-targets --target x86_64-pc-windows-msvc -- -D warnings
    
    - name: 'Test all workspace projects'
      shell: bash
      if: true
      run: |
        cargo test --all --release --features "d3d" --all-targets --target x86_64-pc-windows-msvc -- --nocapture
      
    - name: 'Generate skia-org example images'
      shell: bash
      if: false
      run: |
        cargo run --release --features "d3d" --target x86_64-pc-windows-msvc "${{ env.SKIA_STAGING_PATH }}/skia-org" 
    
    - name: 'Upload skia-org example images'
      if: false
      uses: actions/upload-artifact@v2
      with:
        name: skia-org-images-x86_64-pc-windows-msvc
        path: ${{ env.SKIA_STAGING_PATH }}/skia-org
    
    - name: 'Compress binaries'
      if: true
      uses: master-atul/tar-action@v1.0.2
      with:
        command: c
        cwd: '${{ env.SKIA_STAGING_PATH }}'
        files: 'skia-binaries'
        outPath: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
    
    - name: 'Release binaries'
      if: true
      uses: pragmatrix/release-action@exp
      with:
        owner: rust-skia
        repo: skia-binaries
        allowUpdates: true
        replacesArtifacts: true
        tag: '${{ env.SKIA_BINARIES_TAG }}'
        artifacts: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
        artifactErrorsFailBuild: true
        token: ${{ secrets.RUST_SKIA_RELEASE_TOKEN }}
        prerelease: true
  windows-release-release-d3d-textlayout:
    runs-on: windows-2022
    # Containers are not supported on Windows.
    # container: ghcr.io/pragmatrix/rust-skia-windows:latest
    env: 
      SKIA_DEBUG: 0
    
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    
    - name: Install Rust
      uses: hecrj/setup-rust-action@master
      with:
        rust-version: stable
    
    - name: Install Clippy
      run: rustup component add clippy
      shell: bash
    
    - name: Python Version
      run: python --version
    
    - name: Install LLVM
      run: choco install llvm
    - name: 'Install Rust target x86_64-pc-windows-msvc'
      shell: bash
      run: |
        rustup target add x86_64-pc-windows-msvc
    
    - name: 'Build all targets in skia-safe for x86_64-pc-windows-msvc with features d3d,textlayout'
      shell: bash
      run: |
        if [ "false" == "true" ]; then
          TARGET=x86_64-pc-windows-msvc
          TARGET=${TARGET//-/_}
          export CC_${TARGET}=x86_64-pc-windows-msvc26-clang.exe
          export CXX_${TARGET}=x86_64-pc-windows-msvc26-clang++.exe
          TARGET_UPPERCASE=`echo "${TARGET}" | tr [a-z] [A-Z]`
          export CARGO_TARGET_${TARGET_UPPERCASE}_LINKER=x86_64-pc-windows-msvc26-clang.exe
          echo "Set CC, CXX, and CARGO_LINKER target specific environment variables for Android"
        fi
        if [ "false" == "true" ]; then
          source /emsdk/emsdk_env.sh
        fi
        cargo clean
        cargo build -p skia-safe --all-targets --release --features "d3d,textlayout" --target x86_64-pc-windows-msvc
        echo "SKIA_BINARIES_TAG=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/tag.txt")" >> ${GITHUB_ENV}
        echo "SKIA_BINARIES_KEY=$(cat "${BUILD_ARTIFACTSTAGINGDIRECTORY}/skia-binaries/key.txt")" >> ${GITHUB_ENV}
        echo "SKIA_STAGING_PATH=${BUILD_ARTIFACTSTAGINGDIRECTORY}" >> ${GITHUB_ENV}
      env:
        BUILD_ARTIFACTSTAGINGDIRECTORY: ${{ runner.temp }}
        EMCC_CFLAGS: "-s ERROR_ON_UNDEFINED_SYMBOLS=0 -s MAX_WEBGL_VERSION=2"
    
    - name: 'Run Clippy'
      shell: bash
      if: true
      run: |
        cargo clippy --release --features "d3d,textlayout" --all-targets --target x86_64-pc-windows-msvc -- -D warnings
    
    - name: 'Test all workspace projects'
      shell: bash
      if: true
      run: |
        cargo test --all --release --features "d3d,textlayout" --all-targets --target x86_64-pc-windows-msvc -- --nocapture
      
    - name: 'Generate skia-org example images'
      shell: bash
      if: false
      run: |
        cargo run --release --features "d3d,textlayout" --target x86_64-pc-windows-msvc "${{ env.SKIA_STAGING_PATH }}/skia-org" 
    
    - name: 'Upload skia-org example images'
      if: false
      uses: actions/upload-artifact@v2
      with:
        name: skia-org-images-x86_64-pc-windows-msvc
        path: ${{ env.SKIA_STAGING_PATH }}/skia-org
    
    - name: 'Compress binaries'
      if: true
      uses: master-atul/tar-action@v1.0.2
      with:
        command: c
        cwd: '${{ env.SKIA_STAGING_PATH }}'
        files: 'skia-binaries'
        outPath: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
    
    - name: 'Release binaries'
      if: true
      uses: pragmatrix/release-action@exp
      with:
        owner: rust-skia
        repo: skia-binaries
        allowUpdates: true
        replacesArtifacts: true
        tag: '${{ env.SKIA_BINARIES_TAG }}'
        artifacts: '${{ runner.temp }}/skia-binaries-${{ env.SKIA_BINARIES_KEY }}.tar.gz'
        artifactErrorsFailBuild: true
        token: ${{ secrets.RUST_SKIA_RELEASE_TOKEN }}
        prerelease: true